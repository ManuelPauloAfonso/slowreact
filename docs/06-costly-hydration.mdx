import DocContent from '@/components/DocContent'

<DocContent>

export const meta = {
  num: '06',
  title: 'Costly Hydration',
  tag: 'ssr',
}

<div className="not-prose mb-10 pb-8 border-b border-border">
  <div className="flex items-center gap-3 mb-3">
    <span className="font-mono text-[11px] text-muted-foreground/40 border border-border rounded px-1.5 py-0.5">
      {meta.num}
    </span>
    <span className="font-mono text-[10px] uppercase tracking-wider text-orange-500 border border-orange-500/20 bg-orange-500/5 rounded px-1.5 py-0.5">
      {meta.tag}
    </span>
  </div>
  <h1 className="font-mono text-3xl font-medium tracking-tight text-foreground">
    {meta.title}
  </h1>
</div>

<div className="not-prose mb-8 border-l-2 border-orange-500 pl-4 bg-orange-500/[0.04] rounded-r-lg py-3 pr-4">
  <p className="font-mono text-[10px] uppercase tracking-wider text-orange-500 mb-1">
    Field Notes — Prof. Oak
  </p>
  <p className="font-sans text-sm text-muted-foreground italic leading-relaxed">
    "Magikarp uses Splash! But nothing happened... A component re-rendered 47
    times on a single keystroke. Wild performance issues appeared!"
  </p>
</div>

## What is happening

This component renders a list of items and allows the user to interact with each
one. Open the app, interact with the list, and watch what happens in the React
Developer Tools Profiler.

## Hints

- Not every component in the tree needs to re-render when the parent updates
- React has a way to tell a component to skip re-rendering when its props have not changed
- Functions and objects created inline in the parent get a new reference on every render

<div className="not-prose my-6 bg-muted/50 border border-border rounded-lg p-4">
  <p className="font-mono text-[10px] uppercase tracking-wider text-muted-foreground/40 mb-2">
    Pokédex #025 — React.memo
  </p>
  <p className="font-sans text-sm text-muted-foreground leading-relaxed">
    When this Pokémon detects that its props haven't changed, it skips the
    render entirely. Effective against unnecessary re-render attacks.
  </p>
</div>

## The problem

```tsx
import { useState } from 'react'
import type { listItemProps } from '../../types/ListItemProps'
import { items } from '../../mocks/items'

function ListItem({ item, onSelect, selected }: listItemProps) {
  return (
    <div>
      <span>{item.name}</span>
      <button onClick={() => onSelect(item.id)}>
        {selected ? 'Selected' : 'Not selected'}
      </button>
    </div>
  )
}

export default function UnnecessaryRerenders() {
  const [selectedId, setSelectedId] = useState<number | null>(null)
  return (
    <div>
      <h1>Unnecessary Re-renders</h1>
      {items.map((item) => (
        <ListItem
          key={item.id}
          item={item}
          onSelect={setSelectedId}
          selected={selectedId === item.id}
        />
      ))}
    </div>
  )
}
```

## References

- [react.dev/reference/react/memo](https://react.dev/reference/react/memo)
- [react.dev/reference/react/useCallback](https://react.dev/reference/react/useCallback)
- [debugbear.com/blog/measuring-react-app-performance](https://www.debugbear.com/blog/measuring-react-app-performance)

<div className="not-prose mt-12 pt-6 border-t border-border flex items-center justify-between">
  <span className="font-sans text-sm text-muted-foreground/40">← Previous</span>
  <a
    href="/docs/-the-next-exercise"
    className="bg-orange-500 hover:bg-orange-600 text-white font-mono text-xs font-medium px-4 py-2 rounded-md transition-colors no-underline"
  >
    Next exercise →
  </a>
</div>

</DocContent>
